block content
        h1 #{work.title}
        div
            p #{work.content}
        form(id = 'button',  method='post', action='/edit')
            input(name='adds')
            input(type='submit', value='Edit')
        form(id = 'button',  method='post', action='/finish')
            input(type='submit', value='Finish')